package system;
// Created by User on 28.06.2016.

import javafx.collections.FXCollections;
import javafx.collections.ObservableList;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

public class SelectedExercise {
/*
    private static Exercise selectedExercise;

    public ObservableList<String> getClasses(){
        List<String> list = new ArrayList<>();
        for(Classframe classframe : selectedExercise.getClassframes()){
            list.add(classframe.getClassname());
        }
        return FXCollections.observableList(list);
    }

    public ObservableList<String> getTests(){
        List<String> list = new ArrayList<>();

        for(Classframe testframe : selectedExercise.getTestframes()){
            list.add(testframe.getClassname());
        }
        return FXCollections.observableList(list);


    }

    public static Exercise getSelectedExercise() {
        return selectedExercise;
    }

    public static void setSelectedExercise(Exercise selectedExercise) {
        SelectedExercise.selectedExercise = selectedExercise;
    }
    */
}
